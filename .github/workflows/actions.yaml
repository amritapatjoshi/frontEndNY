name: Build and Test Next JS App

on:
  push:
    branches:
      - 'main'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: 20

    - name: Install npm dependencies
      run: npm install

    - name: Build Next JS App
      run: npm run build 

    - name: Run GitLeaks Scan
      run: |
       wget https://github.com/zricethezav/gitleaks/releases/latest/download/gitleaks-linux-amd64
       chmod +x gitleaks-linux-amd64
       sudo mv gitleaks-linux-amd64 /usr/local/bin/gitleaks
       gitleaks -o gitleaks-report.json
      
       env:
        GITHUB_TOKEN: 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  

    # - name: Run Tests
    #   run: npm test 

